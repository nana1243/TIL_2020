### 여러개의 버튼 추가하기

>1. click함수를 통해 **여러기능** 실행시키기
>
>- 이미지 띄우기
>- 전화하기
>- 웹 브라우저 띄우기
>
>2. intent의 의미 알아두기

1. click 함수만들기 : BUTTON 클릭시 앱에서의 반응에 담당하는 함수를 쓸 수 있다

```java
public void ck(View view) {
        if (view.getId() == R.id.button) {
            imageView.setImageResource(R.drawable.d1);
        } else if (view.getId() == R.id.button2) {
            imageView.setImageResource(R.drawable.d2);
        } else if (view.getId() == R.id.button3) {
            imageView.setImageResource(R.drawable.d3);
        } else if (view.getId() == R.id.button4) {
            Intent intent =
                    new Intent(Intent.ACTION_VIEW,
                            Uri.parse("http://m.naver.com"));
            startActivity(intent);
        } else if (view.getId() == R.id.button4) {
      
        }
```



> 여기서 Intent?
>
> : 어떤 기능을 실행할 것인지 지정할때 사용
>
> ex>
>
> ```java
> // 기능에 해당하는 intent 코드
> Intent intent = new Intent(Intent.ACTION_VIEW, Uri.parse("http://m.naver.com"));
> // 그러한 intent를 activity에서 실행하자
> startActivity(intent);
> 
> ```
>
> 



[기능 with 코드]

1.  그림띄울때

```java
 ImageView imageview;
 imageview=findViewById(R.id.imageView);

imageview.setImageResource(R.drawable.이미지이름)
```



2.웹 브라우저로 가고싶을때

```java
   Intent intent =  new Intent(Intent.ACTION_VIEW, Uri.parse("http://m.naver.com"));
   startActivity(intent); 
```



3.전화기능

```java
    Intent intent =  new Intent(Intent.ACTION_CALL, Uri.parse("tel:010-3333-2222"));
    if (checkSelfPermission(Manifest.permission.CALL_PHONE) != PackageManager.PERMISSION_GRANTED) {
                // TODO: Consider calling
                //    Activity#requestPermissions
                // here to request the missing permissions, and then overriding
                //   public void onRequestPermissionsResult(int requestCode, String[] permissions,
                //                                          int[] grantResults)
                // to handle the case where the user grants the permission. See the documentation
                // for Activity#requestPermissions for more details.
                return;
            }
            startActivity(intent); }
```



